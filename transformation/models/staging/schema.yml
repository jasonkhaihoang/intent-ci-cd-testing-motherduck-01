version: 2

models:
  - name: stg_raw__customers
    description: Staging view over raw.customers — renames columns and type-safes signup_date.
    columns:
      - name: customer_id
        description: Customer primary key.
        tests:
          - not_null
          - unique
      - name: customer_name
        description: Customer name.
      - name: signup_date
        description: Date the customer signed up.
      - name: country
        description: Customer country code.

  - name: stg_raw__sales
    description: Staging view over raw.sales — renames columns and type-safes sale_date.
    columns:
      - name: sale_id
        description: Sale primary key.
        tests:
          - not_null
          - unique
      - name: customer_id
        description: Foreign key to customers.
      - name: product
        description: Product name.
      - name: quantity
        description: Number of units sold.
      - name: unit_price
        description: Price per unit.
      - name: sale_date
        description: Date of sale.
      - name: region
        description: Sales region.
