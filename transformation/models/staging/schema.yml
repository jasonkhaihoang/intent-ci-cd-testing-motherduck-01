version: 2

models:
  - name: stg_raw__customers
    description: Staging view over raw.customers — renames columns and type-safes signup_date.
    columns:
      - name: customer_id
        description: Customer primary key.
        tests:
          - not_null
          - unique
      - name: customer_name
        description: Customer name.
      - name: signup_date
        description: Date the customer signed up.
      - name: country
        description: Customer country code.

  - name: stg_raw__sales
    description: Staging view over raw.sales — type-safes sale_date and computes total_amount.
    columns:
      - name: sale_id
        description: Sale transaction primary key.
        tests:
          - not_null
          - unique
      - name: customer_id
        description: Foreign key to customer.
        tests:
          - not_null
      - name: product
        description: Product name.
      - name: quantity
        description: Number of units sold.
      - name: unit_price
        description: Price per unit.
      - name: total_amount
        description: Computed total (quantity * unit_price).
      - name: sale_date
        description: Date of sale.
      - name: region
        description: Sales region code.
